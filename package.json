{
  "private": true,
  "scripts": {
    "postinstall": "pulp dep install",
    "test": "pulp test && rimraf docs && pulp docs"
  },
  "license": "Apache-2",
  "devDependencies": {
    "moment": "^2.10.6",
    "pulp": "^11.0.0",
    "rimraf": "^2.4.1",
    "xhr2": "^0.1.3"
  }
}
